[pytest]
xfail_strict = False
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    expected_failure: mark test as expected to fail

# Treat xfail tests as passed for CI/CD
# This ensures that tests marked as expected failures don't cause the build to fail
xvs = True 